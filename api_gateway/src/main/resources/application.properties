spring.application.name=api_gateway

# Server configuration
server.port=8080


# PostgreSQL database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/e_voting
spring.datasource.username=anjula
spring.datasource.password=anjula
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate JPA configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Define service URLs (optional if you want to manage it here)
spring.datasource.url=jdbc:mysql://localhost:3306/voting_system_db
spring.datasource.username=stduser
spring.datasource.password=Abc@1234

# Use Hibernate's automatic schema generation
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration